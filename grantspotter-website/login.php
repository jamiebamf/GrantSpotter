<?php require 'bootstrap.php'; require 'partials.php'; $error='';
if($_SERVER['REQUEST_METHOD']==='POST'){verify_csrf();$email=strtolower(trim($_POST['email']??''));$password=$_POST['password']??'';foreach(load_json('users.json') as $u){if($u['email']===$email && password_verify($password,$u['password_hash'])){$_SESSION['user_id']=$u['id'];header('Location: '.(($u['subscription_status']??'Free')==='Active'?'dashboard.php':'subscribe.php'));exit;}}$error='Email or password not recognised.';}
site_header('Login'); ?>
<section class="auth-layout compact"><div class="auth-panel"><a class="back-link" href="index.php">← Back to homepage</a><p class="section-kicker">Welcome back</p><h1>Log in to your dashboard.</h1><?php if($error): ?><div class="alert error"><?= e($error) ?></div><?php endif; ?><form method="post" class="stack-form"><input type="hidden" name="csrf" value="<?= csrf_token() ?>"><label>Email address<input type="email" name="email" required></label><label>Password<input type="password" name="password" required></label><button class="btn btn-primary btn-large full">Log in</button><p class="form-note">New to GrantSpotter? <a href="register.php">Create an account</a></p></form></div><aside class="auth-aside quote"><blockquote>“Everything is in one place, and I can see exactly which grants are worth my time.”</blockquote><span>Community group founder</span></aside></section>
<?php site_footer(); ?>
